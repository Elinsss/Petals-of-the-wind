<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Войти или зарегистрироваться</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>

        .container-login {
            max-width: 800px;
            padding: 2rem;
            border-radius: 10px;
            display: flex;
            justify-content: center; /* Центрируем по горизонтали */
            align-items: center; /* Центрируем по вертикали */
            height: 100vh; /* Высота на весь экран */
            margin: 0 auto; /* Выравнивание по центру по горизонтали */
        }

        .form-section {
            width: 100%;
            max-width: 500px; /* Ограничиваем ширину формы */
            padding: 1rem;
            border-radius: 10px;
            background: #fff;
        }

        .btn-custom {
            background: linear-gradient(45deg, #ff416c, #ff4b2b);
            color: #fff;
            border: none;
            border-radius: 20px;
            padding: 10px 20px;
            width: 100%; /* Кнопки занимают всю ширину */
            margin-bottom: 10px; /* Отступ между кнопками */
        }

            .btn-custom:hover {
                background: linear-gradient(45deg, #ff4b2b, #ff416c);
                transform: scale(1.05);
            }
    </style>
</head>
<body>
    <div class="container-login">
        <div class="form-section">
            <h3 style="text-align: center;"><strong>Вход в аккаунт</strong></h3>
            <hr />
            <form asp-action="login" asp-controller="account" >
                <div class="mb-3">
                    <label for="email" class="form-label">E-mail: *</label>
                    <input type="email" class="form-control" name="email" placeholder="Введите email">
                </div>
                <div class="mb-3">
                    <label for="password" class="form-label">Пароль: *</label>
                    <input type="password" class="form-control" name="password" placeholder="Введите пароль">
                </div>
                <button type="submit" class="btn btn-custom">Войти</button>
                <a href="~/account/register" type="button" class="btn btn-custom">Зарегистрироваться</a>
            </form>
        </div>
    </div>
</body>
</html>
